## Setup

1. Clone the repository.
2. Open `database/setup.sql` in SQL Server Management Studio and run it.
3. Update the connection string in `appsettings.json` if needed.
4. Run the application.

Each run adds a new weather reading to the `WeatherReadings` table.
